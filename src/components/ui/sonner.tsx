import React from "react"
import { toast } from "sonner"

// Componente Toaster simplificado
const Toaster = () => {
  return null // Usando apenas as funções toast, não o componente visual
}

export { Toaster, toast }
